import 'package:flutter/material.dart';

class AdminPlacement extends StatelessWidget {
  const AdminPlacement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}